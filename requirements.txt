# Dependencias Python del prototipo — versiones fijadas para reproducibilidad.
# Recomendado:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt

# --- Núcleo de datos y modelos ---
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
scipy==1.18.0
matplotlib==3.10.8
joblib==1.5.3
shap==0.52.0
networkx==3.6.1
graphviz==0.21
PyYAML==6.0.2

# --- Pruebas ---
pytest==9.0.2

# --- Apache Spark y extensiones ---
pyspark==4.1.1
pyarrow==25.0.0

# Los wrappers Python se fijan a la MISMA versión del núcleo JVM versionado
# en jars/, evitando mezclar APIs distintas con JARs incompatibles.
# PyPI no incluye el core JVM de GraphFrames, por eso ambos JARs continúan
# versionados en jars/ para el entorno local sin resolución Maven.
delta-spark==4.3.0
graphframes-py==0.10.0

# --- Verificación de documentos ya generados ---
python-docx==1.2.0

# Apache Airflow NO se instala aquí. Su runtime separado está declarado en
# `requirements-airflow.txt` y CI lo instala con las constraints oficiales
# correspondientes antes de importar los cuatro DAGs con DagBag. Los DAGs
# ejecutan las tareas ML con `CGR_PROJECT_PYTHON` o `.venv/bin/python`, no con
# el intérprete de Airflow.
#
# SQL Server institucional: `src/connectors/sqlserver.py` importa `pyodbc`
# de forma diferida. El entorno CGR debe instalar pyodbc + el driver ODBC
# aprobado institucionalmente; el PoC/CI no necesita ese driver para operar.
